# Publish — OPC-UA Agent
#   • Push to main touching the agent → BETA image (:X.Y.Z-beta.N, :beta, :main, :sha-<commit>)
#   • Push tag `opcua-agent-v1.2.3`   → RELEASE image (:1.2.3, :1.2, :latest) + GitHub Release
# Next-version source: the "## [Unreleased]" line of src/EdgePulse.OpcUaAgent/CHANGELOG.md
name: Publish — OPC-UA Agent

on:
  push:
    branches: [main]
    paths: ['src/EdgePulse.OpcUaAgent/**']
    tags: ['opcua-agent-v*']

permissions:
  contents: write
  packages: write

jobs:
  meta:
    runs-on: ubuntu-latest
    outputs:
      channel: ${{ steps.m.outputs.channel }}
      version: ${{ steps.m.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # need tags + full history for the beta number
      - id: m
        uses: ./.github/actions/component-version
        with:
          tag-prefix: opcua-agent
          changelog: src/EdgePulse.OpcUaAgent/CHANGELOG.md
          paths: src/EdgePulse.OpcUaAgent

  publish:
    needs: meta
    uses: ./.github/workflows/_publish-image.yml
    with:
      image: opcua-agent
      context: src/EdgePulse.OpcUaAgent
      dockerfile: src/EdgePulse.OpcUaAgent/Dockerfile
      channel: ${{ needs.meta.outputs.channel }}
      version: ${{ needs.meta.outputs.version }}
    secrets: inherit

  release:
    needs: [meta, publish]
    if: github.ref_type == 'tag'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release view "${{ github.ref_name }}" >/dev/null 2>&1 \
            && echo "Release ${{ github.ref_name }} already exists — skipping." \
            || gh release create "${{ github.ref_name }}" --title "OPC-UA Agent ${{ needs.meta.outputs.version }}" --generate-notes
